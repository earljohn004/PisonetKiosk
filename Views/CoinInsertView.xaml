<Window x:Class="PisonetKiosk.Views.CoinInsertView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PisonetKiosk.Views"
        mc:Ignorable="d"
        Title="CoinInsertView" Topmost="True" ResizeMode="NoResize" WindowStartupLocation="CenterScreen" WindowState="Maximized" Opacity="0.75" Background="Black" AllowsTransparency="True" WindowStyle="None">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Column="1" Grid.Row="1">
            <Button  FontSize="40" Content="Top-Up" Width="400" Height="100" Margin="20"  Click="Button_Click_1"/>
            <Button FontSize="40" Content="Create New Account" Width="400" Height="100" Margin="20"/>
            <Button FontSize="40" Content="Login As Guest" Width="400" Height="100" Margin="20"/>

        </StackPanel>
    </Grid>
</Window>
